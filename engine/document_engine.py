class DocumentEngine:

    def process(self, file_path: str):

        """
        Reads the PDF.

        Converts it into raw text.

        Returns raw text.
        """

        raw_text = ""

        return raw_text